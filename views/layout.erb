<!DOCTYPE html>
<html>
<head>
	<title>Shopping API Login/Registration</title>
	<meta name='viewport' content='width=device-width, initial-scale=1'>
</head>
<body>
	<h1>StoreLogoHere</h1>
	<section>
		<% if session[:user] %>
			<strong>Welcome back, <%= @username %></strong>
		<% else %>
			<em>Hello and thanks for stopping by. Consider registering?</em>
		<% end %>
	</section>
	<hr>
	<a href='/'>Home</a> | <a href='/account/supersecret'>Invitation</a>
	| 
	
	<% if session[:user] %>
		<a href='/account/logout'>Logout</a>
	<% else %>
		<a href='/account'>Login/Register</a>
	<% end %>
	<hr>
	<%= yield %>
</body>
</html>